[package]
name = "cpg"
version = "0.1.0"
edition = "2021"

[dependencies]
bio = "3"
clap = { version = "4", features = ["derive"] }
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1" 

[dev-dependencies]
assert_cmd = "2"   # testes de integração do CLI
predicates = "3"